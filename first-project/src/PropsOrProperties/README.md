# PROPERTIES AKA PROPS IN REACT

#### There are two types to represent props(properties)in react.

### FIRST TYPE

### SECOND TYPE


### DATA TYPES TO PASS IN THE PROPS

























**NOTE** 
* The function variable name must be in small or camelCase or with hyphen, but not with space between the letters.
* Unlike JavaScript, React.JS doesn't allow to change the values after assigning in the object-call function. If want then one has to change it before passing in the function at original place.
* The `div` we normally use in Return function in React in this `<></>` form called **Fragment Div**.